<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Wallet_Main.SettingsPage"
    BackgroundColor="#141824"
    Padding="20"
    xmlns:strings="clr-namespace:Wallet_Main.Resources.Strings">

    <ScrollView VerticalScrollBarVisibility="Never">

        <VerticalStackLayout Spacing="25">

            <!-- HEADER -->
            <Label
                Text="{x:Static strings:AppResources.Settings}"
                FontSize="26"
                FontAttributes="Bold"
                TextColor="White"/>


            <!-- WALUTA -->
            <Border
                BackgroundColor="#1C2436"
                Stroke="#2B344A"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 18"
                Padding="18">

                <VerticalStackLayout Spacing="12">

                    <Label
                        Text="{x:Static strings:AppResources.Currency}"
                        FontSize="16"
                        FontAttributes="Bold"
                        TextColor="White"/>

                    <Picker
                        Title="{x:Static strings:AppResources.ChooseCurrency}"
                        ItemsSource="{Binding Currencies}"
                        ItemDisplayBinding="{Binding Code}"
                        SelectedItem="{Binding SelectedCurrency}"
                        
                        TextColor="White"
                        BackgroundColor="#263046"/>

                </VerticalStackLayout>

            </Border>


            


            <Border
    BackgroundColor="#1C2436"
    Stroke="#2B344A"
    StrokeThickness="1"
    StrokeShape="RoundRectangle 18"
    Padding="18">

                <VerticalStackLayout Spacing="16">

                    <!-- TITLE -->
                    <Label
            Text="{x:Static strings:AppResources.AppData}"
            FontSize="16"
            FontAttributes="Bold"
            TextColor="White"/>

                    <!-- ACTIONS -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="12">

                        <!-- IMPORT -->
                        <Border
                Grid.Column="0"
                BackgroundColor="#263046"
                StrokeShape="RoundRectangle 16"
                Padding="16">

                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding ImportCommand}"/>
                            </Border.GestureRecognizers>

                            <VerticalStackLayout
                    HorizontalOptions="Center"
                    Spacing="8">

                                <Border
                        BackgroundColor="#3B82F633"
                        HeightRequest="40"
                        WidthRequest="40"
                        StrokeShape="RoundRectangle 12">

                                    <Image
                            Source="download.png"
                            HeightRequest="20"
                            WidthRequest="20"
                            HorizontalOptions="Center"
                            VerticalOptions="Center"/>
                                </Border>

                                <Label
                        Text="Import"
                        TextColor="White"
                        FontAttributes="Bold"
                                    HorizontalOptions="Center"
                        FontSize="13"/>
                            </VerticalStackLayout>

                        </Border>


                        <!-- EXPORT -->
                        <Border
                Grid.Column="1"
                BackgroundColor="#263046"
                StrokeShape="RoundRectangle 16"
                Padding="16">

                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Export_TransactionsCommand}"/>
                            </Border.GestureRecognizers>

                            <VerticalStackLayout
                    HorizontalOptions="Center"
                    Spacing="8">

                                <Border
                        BackgroundColor="#00BFA533"
                        HeightRequest="40"
                        WidthRequest="40"
                        StrokeShape="RoundRectangle 12">

                                    <Image
                            Source="upload.png"
                            HeightRequest="20"
                            WidthRequest="20"
                            HorizontalOptions="Center"
                            VerticalOptions="Center"/>
                                </Border>

                                <Label
                        Text="Export"
                        TextColor="White"
                        FontAttributes="Bold"
                                    HorizontalOptions="Center"
                        FontSize="13"/>



                            </VerticalStackLayout>

                        </Border>

                    </Grid>

                </VerticalStackLayout>

            </Border>

        </VerticalStackLayout>

    </ScrollView>

</ContentPage>